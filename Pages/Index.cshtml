@page
@model IndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="card p-4 bg-white shadow-sm mb-4">
    <h2 class="mb-4">📄 智慧履歷分析</h2>

    <form method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label class="form-label">上傳 PDF 履歷：</label>
            <input type="file" asp-for="ResumeFile" accept=".pdf" class="form-control" />
        </div>

        <div class="mb-3">
            <label class="form-label">或直接貼上履歷文字：</label>
            <textarea asp-for="ResumeText" rows="6" class="form-control"></textarea>
        </div>

        <div class="mb-3">
            <label class="form-label">目標職缺說明：</label>
            <textarea asp-for="JobDescription" rows="4" class="form-control"></textarea>
        </div>

        <button type="submit" class="btn btn-primary">🚀 開始分析</button>
    </form>
</div>

<div id="contact" class="card p-4 bg-white shadow-sm mb-4">
    <h4>聯絡資訊</h4>
    <p>Email: brianct1217@gmail.com</p>
    <p>Linkedin: <a href="https://www.linkedin.com/in/ching-teng-hsiao" target="_blank">ching-teng-hsiao</a></p>
    <p>GitHub: <a href="https://github.com/teng1217" target="_blank">teng1217</a></p>
</div>

<div id="feedback" class="card p-4 bg-white shadow-sm mb-4">
    <h4>使用者意見回饋</h4>
    <form method="post" action="mailto:brianct1217@gmail.com" enctype="text/plain">
        <div class="mb-3">
            <label class="form-label">您的建議：</label>
            <textarea name="message" rows="4" class="form-control" required></textarea>
        </div>
        <button type="submit" class="btn btn-success">送出意見</button>
    </form>
</div>